package io.chenxi.easyplan.rpc

class ProtoMapper {
    companion object {
//        fun asRpcObjectArray(dates: ArrayList<String>, rxData: ArrayList<Float>, txData: ArrayList<Float>): DataUploadRequest {
//            val numSamples = dates.size
//            if (numSamples != rxData.size || numSamples != txData.size)
//                throw IllegalArgumentException("Input arrays should have the same size")
//            val builder = DataUploadRequest.newBuilder()
//            for (i in 0 until numSamples) {
//                builder.addData(i, DataUsageStats.newBuilder().setDate(dates[i]).setRx(rxData[i]).setTx(txData[i]).build())
//            }
//            return builder.build()
//        }
    }
}