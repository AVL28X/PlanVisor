<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="1dip">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@mipmap/ic_att"
        android:padding="4dp"
        android:layout_alignParentStart="true"
        android:contentDescription="@string/image_desc_default" />

    <LinearLayout
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:padding="2dp"
        android:layout_toEndOf="@+id/icon"
        android:layout_toStartOf="@+id/item_list_plan_price"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/item_list_plan_name"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:text="@string/item_plan_name_default"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/item_list_plan_desc"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:ellipsize="marquee"
            android:maxLines="3"
            android:text="@string/item_plan_desc_default"
            android:textSize="12sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/item_list_plan_price"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_alignParentEnd="true"
        android:gravity="center"
        android:text="@string/item_plan_price" />


</RelativeLayout>